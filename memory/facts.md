---
name: facts
description: Project-specific facts, system state, architecture notes
metadata:
  type: project
---

# Facts

## Project State

- [2026-08-22] This is a fresh template project — all bootstrap files (IDENTITY.md, SOUL.md, CONTRACT.md, USER.md, TOOLS.md) contain unfilled placeholder content. No real identity or user data has been entered yet. (source: session 2026-08-22, confidence: high)
- [2026-08-23] IDENTITY.md, SOUL.md, CONTRACT.md are now filled in with the agent's real identity — name "Nic" ("Agentul Nic"), personal 24/7 assistant to Nic (see [[corrections]] — not "Nick") working via Slack, Romanian, friendly-direct tone. See git commit `6c97072`. (source: session 2026-08-23, confidence: high)
- [2026-08-24] (Updated, previously: "USER.md, TOOLS.md, DECISIONS.md, GROUND-TRUTH.md are still template placeholders") USER.md, TOOLS.md and CONTRACT.md are now also filled in with real content — only DECISIONS.md and GROUND-TRUTH.md remain empty templates. USER.md: user is "Nic" (see [[corrections]] — not "Nick"), Europe/Bucharest, active 08:00-22:00 (no non-urgent updates after), works in parallel on this agent, nic-copywriting/eBunaasta (coaching/evaluare copywriting app), and an eBunaasta sales landing page (educație financiară/investiții); prefers concise-with-context updates, confirmation before high-impact actions, and no infinite-loop diagnostics. TOOLS.md: Slack bot via SLACK_BOT_TOKEN in `.env`, channel `#asistent-nic` (SLACK_CHANNEL_ID), authorized chat via SLACK_ALLOWED_USER; Windows native (no WSL2), Git for Windows bash, Windows Terminal, jq via scoop; no MCP servers configured; no access to external accounts/money/other undocumented directories. CONTRACT.md: acts autonomously on Slack replies, daily memory updates, heartbeats, and local script/diagnostic runs; asks first on tasks stuck >10min, changes outside the agent directory, or anything published/sent externally; never pushes to git, deletes files, or touches money without explicit confirmation. (source: direct file read 2026-08-24, confidence: high)

(See [[corrections]] for the user-name fix and [[patterns]] for the `.env` sourcing bug fix — details moved there to avoid duplication.)

- [2026-08-22] config.json has 2 crons configured: 1m interval for Slack check, 30m interval for heartbeat. (source: session 2026-08-22, confidence: high)

- [2026-08-22] First session came online and sent Slack notification successfully. (source: memory/2026-08-22.md, confidence: high)

- [2026-08-22] Project runs on Windows via Windows Task Scheduler + Git Bash restart loop (not macOS launchd/tmux). See CLAUDE.md Persistence section. (source: CLAUDE.md, confidence: high)

- [2026-08-25] Health/watchdog system properly ported to Windows: `supervisor.sh` now checks liveness via a PID stored in `session.marker` + `ps -p` (was originally Mac-oriented, checking `tmux has-session`/`launchctl`, effectively unused on Windows). `agent-start-windows.sh` simplified to: launch the terminal window, then run `supervisor.sh` in a loop at `tick_seconds` (240s, see `config.json` → `supervisor`). `config.json`'s `supervisor.auto_restart` was flipped `false → true` in this same change — a higher-impact, harder-to-reverse setting, so the agent asked Nic for explicit confirmation before treating it as settled; Nic's "fa commit" on 2026-08-25 was taken as that confirmation. Daily status report (`scripts/daily-report.sh`) is now active via `HEARTBEAT.md`, firing once per day at/after 22:00, tracked so it only sends once daily. All bundled in commit `7fea14e feat: portare health/watchdog pe Windows, fix diacritice, raport zilnic`. (source: session 2026-08-25, commit 7fea14e, confidence: high)
- [2026-08-25] Security hardening added: `scripts/scan-injection.sh` runs automatically on Slack text messages and downloaded documents (invoked from `check-slack.sh`) to flag prompt-injection patterns — a signal, not an infallible filter, final judgment stays with the agent. `guard-dangerous.py` is a PreToolUse hook that alerts on Slack for risky shell commands (`rm -rf`, `git push --force`, reading `.env` combined with an external request, credential exfiltration patterns, changes to the auto-start mechanism). Also established (and written into `CONTRACT.md`) a standing rule: every file modify/write/delete permission request must show a color-coded risk level (🔴 high / 🟡 moderate / 🟢 low) and propose adjacent solutions including backups. Commit `3d38af3 feat: securitate anti-prompt-injection + alertare comenzi riscante` (made by a parallel agent session — see [[patterns]] "Parallel sessions"). (source: session 2026-08-25, commit 3d38af3, confidence: high)

## System Architecture

- Memory type: `project-root` — MEMORY.md and topic files live in the project root directory.
- Memory path: `/d/backup 11.04.2023/D/Mentorat Danut/mister-o-template-slack/`
- Dream config: `~/.claude/skills/dream/.dream-config` → `DREAM_MEMORY_TYPE=project-root`
- Dream trigger threshold in `.claude/skills/dream/should-dream.sh` is 7 days (168h), not 24h — changed 2026-08-25 (see [[patterns]]).
- [2026-09-03] Dream's global auto-trigger (Stop hook + `~/.claude/skills/dream/should-dream.sh` + `~/.claude/skills/dream/.dream-config`) is **not installed** on this machine — `~/.claude/skills/dream/` doesn't exist and `~/.claude/settings.json` has no `dream`-related Stop hook entry (only a chat-namer hook). The project's own `.claude/skills/dream/should-dream.sh` exists but nothing calls it automatically. All dream runs so far have been manual/external invocations — see [[patterns]] "Dream consolidation cadence" and "Parallel agent sessions" for the 2026-09-03 consequences (7 manual runs in one day, in overlapping bursts). Canonical `.last-dream` for this project lives at the project root (not `memory/.last-dream`). (source: direct inspection 2026-09-03, confidence: high)
- [2026-09-03, IMPORTANT — data-loss risk] `.gitignore` line 8 excludes `memory/*` (with only `!memory/.gitkeep` re-included), which means **`memory/facts.md`, `memory/patterns.md`, `memory/corrections.md`, and every daily `memory/YYYY-MM-DD.md` file have never once been committed to git** — confirmed with `git log --all -- memory/patterns.md` (empty). Only `MEMORY.md` at the project root is actually version-controlled; every dream consolidation so far that appeared to "commit memory" only ever committed the index file, while the substantive topic-file content (everything this and prior dream runs wrote) has existed solely on this machine's local disk the whole time, with no backup and no history. This is a real risk: a disk failure, a `git clean`, or working from a fresh clone on another machine would silently lose all of it. Not fixed by this session — flagged for the user to decide: either remove `memory/*` from `.gitignore` (git-track the topic files) or set up a separate backup for that directory. (source: `git log --all` + `.gitignore` read, 2026-09-03, confidence: high)
